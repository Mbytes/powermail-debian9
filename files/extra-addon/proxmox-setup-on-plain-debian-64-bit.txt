#!/bin/sh


## do not do on remote debian server...be careful. as kernel get updated

## notes from https://pve.proxmox.com/wiki/Install_Proxmox_VE_on_Debian_Jessie

echo "deb [arch=amd64] http://download.proxmox.com/debian jessie pve-no-subscription" > /etc/apt/sources.list.d/proxmox-community.list
curl -s  "http://download.proxmox.com/debian/key.asc" | apt-key add -

apt-get update && apt-get dist-upgrade

apt-get install proxmox-ve ssh postfix ksm-control-daemon open-iscsi systemd-sysv

#apt-get remove os-prober
#apt-get remove linux-image-amd64 linux-image-3.16.0-4-amd64 linux-base
#update-grub

#apt-get install xfce4 lightdm
apt-get purge network-manager

## add in /etc/hosts
# 192.168.1.254 pvelocalhost

