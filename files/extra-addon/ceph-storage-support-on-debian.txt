#!/bin/sh


### do not install this on proxmox server ..as proxymox ceph is different way to install
echo "eb https://download.ceph.com/debian-jewel/ jessie main" >> /etc/apt/sources.list.d/ceph-source.list

curl -s https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -

apt-get update
apt-get -y install ceph


